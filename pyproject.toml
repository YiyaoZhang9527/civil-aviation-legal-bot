[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "legalbot"
version = "0.1.0"
description = "劳动法多 Agent Agentic RAG 系统"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]

